import { expect, test } from '@playwright/test';
test('demo search vertical slice',async({page})=>{await page.goto('');await expect(page.getByRole('heading',{name:/전국 탁구 선수/})).toBeVisible();await page.getByLabel('선수 검색').fill('김탁구');await page.getByRole('button',{name:'검색'}).click();await expect(page.getByRole('heading',{name:'“김탁구” 선수'})).toBeVisible();await expect(page.getByText('2건')).toBeVisible();await expect(page.getByText(/같은 이름의 선수가 여러 명/)).toBeVisible();const candidates=page.getByRole('region',{name:'선수 검색 결과 목록'});await expect(candidates.getByRole('heading',{name:'김탁구'})).toHaveCount(2);await candidates.getByRole('link',{name:'상세 보기'}).first().click();await expect(page.getByRole('heading',{name:'대회 이력'})).toBeAttached();await expect(page.getByText('2026 가상 전국오픈')).toHaveCount(2);await page.getByRole('tab',{name:'출처 비교'}).click();await expect(page.getByRole('heading',{name:'출처별 기록 차이'})).toBeVisible();});
