---
summary: "현재 BUSU 구현의 사용자 흐름, 도메인 규칙, 출처 상태와 수용 조건을 정의한다."
read_when:
  - 기능을 추가하거나 현재 제품 동작을 확인할 때
  - 부수·입상·동명이인·출처 갱신 규칙을 변경할 때
title: "BUSU 제품 스펙"
---

# BUSU 제품 스펙

## 1. 문서 상태

- 기준 시점: 2026-08-12
- 상태: 구현된 MVP 기준
- 제품 언어: 한국어
- canonical implementation: `apps/web`, `packages/*`, `supabase`
- 제품 원칙: BUSU는 공식 부수를 판정하지 않고 공개 대회 기록의 판단 근거를 모은다.

코드와 이 문서가 다르면 테스트된 코드 동작을 확인하고, 의도된 변경이면 같은 작업에서 이 문서를 갱신한다.

## 2. 사용자와 핵심 문제

주 사용자는 대회 참가 전 상대나 참가자의 최근 관측 부수·소속·입상 기록을 확인하려는 탁구인과 운영자다. 정보가 여러 대회 사이트에 분산되어 있고 출처별 체계가 다르므로, BUSU는 다음 정보를 한 화면에 모은다.

- 이름이 같은 선수 후보
- 기록 기반 추정 지역과 당시 소속
- 최근 관측 부수와 부수 체계
- 전체 참가 이력과 4강 이상 입상 이력
- 출처별 차이, 마지막 확인 시각, 원문 URL

## 3. 범위와 비목표

### 현재 범위

- 이름 검색과 동명이인 후보 분리
- 부수 체계별 최근 관측값 요약
- 활성 출처별 실시간 조회 진행 표시
- 선수 상세의 최신순 대회 이력, 입상 필터, 출처 비교
- demo, local live, Supabase production 실행 모드
- 공개 HTTP 출처 adapter와 synthetic fixture 검증
- Supabase RLS/public view/RPC/Edge Function
- GitHub Pages 및 Supabase 배포 자동화

### 비목표

- 공식 부수 확정 또는 자동 승급 판정
- 이름만으로 선수 자동 병합
- 로그인·회원가입·결제·광고
- BAND scraping, CAPTCHA 또는 접근제어 우회
- 전화번호, 이메일, 전체 생년월일, 주소 수집
- 원본 HTML·이미지·PDF 영구 저장

## 4. 사용자 흐름

### 4.1 홈

1. 사용자는 선수 이름을 입력한다.
2. 검색 출처 요약은 활성 출처 수와 전체 공개 출처 수를 작게 보여준다.
3. `상세`를 열면 출처명, 상태, 원문 URL을 목록으로 보여준다.
4. BAND는 사용자 출처 목록에서 제외한다.
5. 운영 수집이 비활성인 에어핑퐁·오케이핑퐁은 BUSU에 결과를 복제하지 않고 검색어가 포함된 원문 검색 링크를 제공한다.

### 4.2 검색 결과

1. 저장된 후보를 먼저 조회한다.
2. production refresh가 켜져 있으면 이름이 2자 이상일 때 활성 출처를 각각 조회한다.
3. 화면은 전체/완료/조회 중 출처 수와 현재 출처명을 독립적으로 갱신한다.
4. 출처 하나가 실패해도 저장 결과와 다른 출처 결과를 유지한다.
5. 상단 `현재 추정 부수`는 카드가 아니라 가로 2행 표로 체계와 `부수·건수`를 표시한다. 좁은 화면에서는 이 표만 가로 스크롤한다.
6. 후보 수 단위는 `명`이 아니라 `건`을 사용한다.
7. 결과 제목에는 `후보`라는 단어를 붙이지 않는다.
8. 동명이인이 2건 이상이면 소속과 활동 지역 확인 경고를 표시한다.
9. 후보 목록은 기본 `입상` 탭과 `출전` 탭으로 나누며 각 탭에 후보 수를 표시한다.
10. 4강 이상 입상 기록이 한 건 이상인 후보는 `입상`, 입상 기록 없이 참가 기록만 있는 후보는 `출전`에 표시한다.
11. `이름 지역` 검색은 첫 단어를 선수 이름, 나머지를 지역 필터로 해석한다. 외부 출처에는 선수 이름만 전달한다.
12. 입상 후보는 최신 입상부터 등수와 대회일을 최대 두 건 표시하고 남은 기록 수를 함께 표시한다.
13. 반복되는 상세 버튼 없이 후보 카드 전체를 해당 후보 상세 링크로 제공한다.
14. 같은 이름 후보가 2건 이상이면 참여자는 `내 기록 구분 돕기`에서 자신의 기록을 여러 개 선택할 수 있다.
15. 참여자는 휴대폰 번호나 생년월일이 아닌 본인이 정한 비공개 숫자 4자리와 선택적 참고사항을 제출한다.
16. 제보 접수만으로 후보를 병합하거나 `동일인 확인됨`으로 변경하지 않고 관리자 검토 대기로 둔다.

### 4.3 선수 상세

- 최근 관측 부수, 통합부수 기록, 4강 이상 입상 건수를 요약한다.
- `전체 이력`, `입상 이력 (4강 이상)`, `출처 비교`, `대회 부수검증` 탭을 제공한다.
- 대회 이력은 desktop table과 mobile card로 표시한다.
- 각 기록은 당시 소속·부수·결과·출처 원문을 제공한다.
- 대회 부수검증은 준비 중이며 자동 확정 판정으로 표현하지 않는다.

## 5. 기능 요구사항

| ID    | 요구사항                                                                                                             | 현재 상태 |
| ----- | -------------------------------------------------------------------------------------------------------------------- | --------- |
| F-001 | 이름 검색은 앞뒤 공백을 제거하고 빈 검색을 실행하지 않는다.                                                          | 구현      |
| F-002 | 이름이 같아도 출처 identity와 소속/대회 근거가 다르면 별도 후보로 유지한다.                                          | 구현      |
| F-003 | 실제 공개 기록과 가상 demo 데이터를 badge로 구분한다.                                                                | 구현      |
| F-004 | 검색 결과는 저장 데이터 유무와 무관하게 활성 출처를 다시 확인할 수 있다.                                             | 구현      |
| F-005 | 출처별 조회 상태와 성공·실패·건너뜀 결과를 독립 표시한다.                                                            | 구현      |
| F-006 | 부수 요약은 체계와 값이 같은 후보를 묶고 건수를 표시한다.                                                            | 구현      |
| F-007 | 선수 기록은 대회일·게시일 기준 최신순으로 표시한다.                                                                  | 구현      |
| F-008 | 입상 탭과 집계는 4강 이상만 포함한다.                                                                                | 구현      |
| F-009 | 사용자가 원문 출처로 이동할 수 있다.                                                                                 | 구현      |
| F-010 | 홈 출처 목록은 계속 늘어날 수 있는 접이식 목록이다.                                                                  | 구현      |
| F-011 | 대회별 최소 출전 가능 부수를 규칙 근거와 함께 검증한다.                                                              | 준비 중   |
| F-012 | 운영자가 동명이인 merge/split을 감사 이력과 함께 검토한다.                                                           | 미구현    |
| F-013 | 검색 후보를 4강 이상 입상 유무에 따라 `입상`과 `출전` 탭으로 나눈다.                                                 | 구현      |
| F-014 | 이름 뒤에 지역을 입력해 동명이인 후보를 부분 일치로 좁힌다.                                                          | 구현      |
| F-015 | 검색 카드에 입상 등수와 대회일을 최신순으로 표시한다.                                                                | 구현      |
| F-016 | 홈·검색 결과·선수 상세가 화면 내용에 맞는 title, description, canonical, Open Graph와 Twitter 메타데이터를 제공한다. | 구현      |
| F-017 | 참여자가 같은 이름의 본인 기록 후보를 선택하고 비공개 구분 코드로 관리자 검토를 요청한다.                            | 구현      |

## 6. 도메인 규칙

### 6.1 선수 identity

- 검색 결과의 같은 이름은 같은 사람이라는 뜻이 아니다.
- 출처별 identity는 source external ID를 우선하고, 없으면 정규화 이름과 소속 또는 대회 근거로 분리한다.
- 자동 merge는 금지한다. 향후 관리자 검토가 있어야 canonical player를 합칠 수 있다.
- 지역 추정값은 identity 병합의 단독 근거가 될 수 없다.
- 참여자는 본인 후보의 소속·활동 지역 수정 또는 동명이인 분리를 근거와 함께 제보한다.
- 참여자 구분 코드는 숫자 4자리 원문을 저장하지 않고 정규화 이름과 함께 서버 HMAC으로 변환한다. 휴대폰 번호·생년월일은 입력하지 않는다.
- 같은 정규화 이름에 속한 후보만 한 제보로 묶을 수 있고, 24시간 중복 요청과 과도한 제출을 제한한다.
- 관리자는 출처 원문을 확인해 제보를 승인·반려하고 이전 값, 근거 URL, 처리자를 감사 이력에 남긴다.
- 승인된 대표 소속·활동 지역은 원문 수집 기록을 덮어쓰지 않고 canonical metadata로 별도 관리한다.

### 6.2 지역

- 출처가 제공한 명시적 지역을 우선한다.
- 없으면 대회명·종목명에서 도·특별시·광역시·특별자치도, 시·특례시·군·구를 정규표현식으로 추출한다.
- 일부 접미사 생략 지명은 검토된 별칭 사전만 사용한다.
- AI 추론은 사용하지 않으며 UI에는 `기록 기반 추정`이라고 표시한다.

### 6.3 기록 시간축

정렬 키는 다음 순서를 사용한다.

1. 대회 개최일 `tournaments.held_on`
2. 출처 게시일 `results.source_published_on`
3. 마지막 확인 시각 `results.last_checked_at`

날짜가 없는 기록은 날짜가 있는 기록 뒤에 둔다. 게시일을 쓴 경우 UI에 `게시일`을 표시한다.

### 6.4 입상

- 입상: 우승, 준우승, 1위, 2위, 3위, 2강, 4강
- 참가 이력: 8강 이하, 예선/본선 진출, 조별 결과, 단순 참가
- 참가 이력도 전체 이력에는 남지만 입상 건수에는 포함하지 않는다.
- 검색 요약은 각 입상의 `rank`와 `대회일 → 게시일` 기준 날짜를 최신순으로 제공한다.

### 6.5 부수 체계

판정 우선순위는 다음과 같다.

1. `T1`~`T7` 또는 `디비전` 명시 → `division`
2. 참가 종목에 `여자` 또는 `여성` 포함 → `women`
3. 대회·종목에 `오픈` 명시 → `open`
4. `지역부수` 체계가 명시됨 → `regional`
5. 일반 숫자 또는 문자 부수 → `integrated`
6. 부수 값이 없거나 해석 불가 → `unknown`

시·군·구 대회라는 이유만으로 지역부수로 분류하지 않는다. 일반 숫자 부수는 통합부수가 기본이다. 동일 숫자라도 서로 다른 체계는 합산하지 않는다.

## 7. 출처 상태

| 코드          | 공개 이름           | adapter          | 운영 상태                | 비고                                                           |
| ------------- | ------------------- | ---------------- | ------------------------ | -------------------------------------------------------------- |
| `astree`      | 애즈트리            | HTTP             | 활성 opt-in              | 최대 2페이지                                                   |
| `ttadivision` | 대한탁구협회 디비전 | HTTP             | 활성 opt-in              | T1~T7, 휴대폰/RT점수 폐기                                      |
| `mytt`        | 마이티티            | HTTP/JSF         | 활성 opt-in              | 단기 session과 ViewState 비저장                                |
| `superstar`   | 슈퍼스타탁구        | HTTP GET         | 활성 opt-in              | 개인별 결과만 저장, 레이팅 제외                                |
| `airping`     | 에어핑퐁            | HTTP             | parser 준비, 운영 비활성 | 재사용 승낙 필요                                               |
| `okpingpong`  | 오케이핑퐁          | HTTP             | parser 준비, 운영 비활성 | 재사용 승낙 필요                                               |
| `iping`       | 아이핑              | 서버 인증형 HTTP | 운영 비활성              | 전용 계정 Secret 설정 후 전국오픈·시군구 입상과 출전 이력 조회 |
| `band`        | 밴드                | 없음             | 사용자 목록 제외         | scraping 금지                                                  |
| `mock`        | 가상 출처           | fixture          | 개발 전용                | 실제 데이터와 badge로 구분                                     |

출처별 세부 URL·parser version·정책 근거는 [출처 메모](source-notes.md)가 기준이다.

## 8. 갱신과 중복 방지

- 브라우저는 외부 출처를 직접 호출하지 않고 `refresh-player` Edge Function을 호출한다.
- 검색 화면은 활성 출처별 요청을 분리해 진행 상태를 보여준다.
- 현재 사용자 검색 요청은 `force=true`로 같은 이름의 6시간 결과 cache를 우회한다.
- 서버는 모든 요청에 출처별 최소 호출 간격(기본 2초)을 적용한다.
- `CRAWL_LIVE`, 출처별 환경 변수, DB `sources.enabled`가 모두 true여야 외부 요청한다.
- adapter가 날짜 범위 검색을 제공하지 않으면 공개 검색 결과를 다시 받아 natural/content hash로 비교한다.
- 신규·변경 기록만 insert/update하고 동일 기록은 확인 시각만 갱신한다.
- 사라진 기록은 즉시 삭제하지 않는다.

## 9. 실행 모드

| 모드       | 선택 조건                                                | repository                 |
| ---------- | -------------------------------------------------------- | -------------------------- |
| Test/Demo  | test, `VITE_APP_MODE=demo`, 또는 Supabase 공개 설정 누락 | `DemoPlayerRepository`     |
| Local live | Vite dev + `VITE_DEV_LIVE_SEARCH=true`                   | `DevLivePlayerRepository`  |
| Production | URL/publishable key 존재, demo 아님                      | `SupabasePlayerRepository` |

production에서 실시간 갱신 UI는 `VITE_SOURCE_REFRESH_ENABLED=true`일 때만 켜진다.

## 10. 보안과 개인정보

- 브라우저에는 Supabase URL과 publishable key만 둔다.
- service role key, Supabase PAT, crawler secret은 Edge/CI trusted environment에만 둔다.
- Edge Function은 허용된 publishable key를 확인하고 내부 DB 작업은 service role로 수행한다.
- 외부 응답은 runtime schema 검증 후 필요한 공개 필드만 정규화한다.
- 전화번호, 이메일, 전체 생년월일, 주소, 원본 응답 본문은 저장하지 않는다.
- 로그인, CAPTCHA, 접근제어를 우회하지 않는다.

## 11. 품질과 배포

모든 변경은 다음 게이트를 통과해야 한다.

```bash
pnpm lint
pnpm typecheck
pnpm test
pnpm build
```

parser 변경은 synthetic fixture test가 필수다. 주요 사용자 흐름은 Playwright smoke로 검증한다. `main` push는 GitHub Pages를 빌드하고, production secret/variable이 준비된 경우 Supabase migration과 Edge Function을 배포한다.

## 12. 현재 제약과 다음 단계

- 출처 운영 정책은 바뀔 수 있어 활성화 근거를 주기적으로 재검토해야 한다.
- 사용자 검색의 `force=true`는 공개 abuse control을 강화하기 전까지 출처별 최소 호출 간격에 의존한다.
- 동명이인 관리자 merge/split UI와 감사 이력은 아직 없다.
- 대회 부수검증 엔진은 준비 중이다.
- 정정 요청과 운영자 업로드는 abuse control과 개인정보 정책 이후에 추가한다.

## 13. 수용 조건

- `김현동` 같은 동명이인 검색에서 후보가 별도 건으로 표시된다.
- 상단 부수 요약은 한 개의 compact 2행 표이며 세로 카드 목록이 아니다.
- 시·군·구 대회의 일반 `5부`, `6부`는 통합부수로 표시된다.
- 참가 종목에 `여자`가 있으면 내부적으로 여자 종목 subtype을 보존하고 화면에는 `통합부수 여자6부` 형식으로 표시한다.
- T1~T7 기록은 디비전부수로 유지된다.
- 4강 이상만 입상 건수에 포함된다.
- 검색할 때 활성 출처 수와 현재 조회 중인 출처를 확인할 수 있다.
- 출처 실패가 다른 출처와 저장 결과를 숨기지 않는다.
- 모든 실제 기록은 원문 URL과 마지막 확인 시각을 제공한다.
