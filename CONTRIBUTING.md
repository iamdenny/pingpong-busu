# 기여 안내

Node 24와 pnpm 11.17을 사용합니다. 기능 변경에는 관련 테스트와 문서를 포함하고, 외부 parser 변경에는 합성 fixture 테스트를 추가합니다. `feat:`, `fix:`, `test:`, `docs:` 등 Conventional Commits를 사용합니다. PR 전 `pnpm lint && pnpm typecheck && pnpm test && pnpm build`를 실행하세요. 실출처 활성화 변경은 수집 정책과 접근 허용 근거를 함께 검토해야 합니다.
